<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gray-100 min-h-screen flex items-center justify-center">
  <div class="max-w-md w-full bg-white rounded-lg shadow-lg p-8 text-center">
    <div class="mb-6">
      <% if (title && title.includes('404')) { %>
        <i class="fas fa-search text-6xl text-gray-400 mb-4"></i>
        <h1 class="text-4xl font-bold text-gray-800 mb-2">404</h1>
        <h2 class="text-xl font-semibold text-gray-600 mb-4">Page Not Found</h2>
      <% } else { %>
        <i class="fas fa-exclamation-triangle text-6xl text-red-400 mb-4"></i>
        <h1 class="text-4xl font-bold text-gray-800 mb-2">Oops!</h1>
        <h2 class="text-xl font-semibold text-gray-600 mb-4">Something went wrong</h2>
      <% } %>
    </div>

    <% if (message) { %>
      <div class="bg-gray-50 rounded-lg p-4 mb-6">
        <p class="text-gray-700"><%= message %></p>
      </div>
    <% } %>

    <div class="space-y-3">
      <a href="/dashboard" class="block w-full bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition">
        <i class="fas fa-home"></i> Go to Dashboard
      </a>
      <a href="javascript:history.back()" class="block w-full bg-gray-200 text-gray-700 px-6 py-3 rounded-lg hover:bg-gray-300 transition">
        <i class="fas fa-arrow-left"></i> Go Back
      </a>
    </div>
  </div>
</body>
</html>
